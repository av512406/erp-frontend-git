node_modules
dist
.git
.env

# Large files and backups
*.tar.gz
*.tar
db_backups/
attached_assets/

# Legacy and source directories not needed in context if using multi-stage correctly (though we need source for build)
# We keep client/server/shared for the build stage, but exclude others
frontend/
backend/

# Config and scripts
.replit
.github/
tests/
test_*
*.sh
!backup.sh
*.md
verify_*.js
setup_*.sh
